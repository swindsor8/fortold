# fortold
Fortold is an AI-assisted platform that turns datasets into structured machine learning plans. It uses AI to propose models and strategies, requires human approval, then executes training safely and transparently, enabling accurate, explainable predictions without needing data science expertise.
